import json
from tqdm import tqdm


# start_url_json = []
# with open('../../sitemapURLs/nrc-rough-sitemap.json') as json_file:
#     rough_sitemap = json.load(json_file)
#     for item in tqdm(rough_sitemap['hits']):
#         start_url_json.append({'url': item['source']['url']})
#
# with open('../../sitemapURLs/nrc-sitemap.json', 'w') as outfile:
#     json.dump(start_url_json, outfile)
